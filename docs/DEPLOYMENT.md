# 배포 및 개발 가이드

## 🌐 배포 정보

- **URL**: https://miryang-daeunmummu.vercel.app/
- **플랫폼**: Vercel
- **저장소**: https://github.com/ekdmssld/bus_time

---

## 🚀 로컬 실행 방법

### 저장소 클론
```bash
git clone https://github.com/ekdmssld/bus_time.git
cd bus_time
```

### 로컬 서버 실행

**Python 3**
```bash
python3 -m http.server 8080
```

**Node.js**
```bash
npx serve
```

브라우저에서 `http://localhost:8080` 접속

### 모바일에서 테스트
1. 컴퓨터와 휴대폰이 같은 Wi-Fi에 연결
2. 컴퓨터 IP 확인: `ifconfig | grep "inet "`
3. 휴대폰 브라우저에서 `http://[IP주소]:8080` 접속

---

## 📝 시간표 수정

시간표 변경 시 `data/schedules.json` 파일만 수정하면 됩니다.

### 새 버스 추가 예시
```json
{
  "order": 35,
  "origin": "교동",
  "departureTime": "23:30",
  "yeongnamru": "23:43",
  "miryangStation": "23:50",
  "pnu": "00:02",
  "destination": "부산대",
  "routeName": "1번",
  "note": "막차"
}
```

### 필드 설명

| 필드 | 설명 |
|------|------|
| order | 순서 |
| origin | 기점 (출발 정류장) |
| departureTime | 기점 출발 시간 |
| yeongnamru | 영남루 시간 |
| miryangStation | 밀양역 시간 |
| pnu | 부산대 시간 |
| destination | 종점 |
| routeName | 노선명 (1번, 2번 등) |
| note | 비고 (신도로, 구도로 등) |

### 방학 기간 설정
`meta.vacationPeriods` 배열 수정:

```json
"vacationPeriods": [
  { "start": "2026-07-01", "end": "2026-08-31" },
  { "start": "2026-12-20", "end": "2027-02-28" }
]
```

---

## 📁 프로젝트 구조

```
bus/
├── index.html              # 메인 HTML (단일 페이지)
├── css/
│   └── style.css           # 스타일시트
├── js/
│   ├── app.js              # 메인 앱 로직 (상태 관리, 렌더링)
│   ├── schedule.js         # 시간표 데이터 처리
│   ├── filter.js           # 필터링 로직
│   ├── favorites.js        # 즐겨찾기 (LocalStorage)
│   └── utils.js            # 유틸리티 함수
├── data/
│   ├── schedules.json      # 버스 시간표 데이터
│   ├── weekday.jpeg        # 평일 시간표 이미지
│   └── withoutweekday.jpeg # 주말/방학 시간표 이미지
└── docs/
    ├── PLANNING.md         # 기획서
    ├── DEVELOPMENT.md      # 개발 문서
    └── DEPLOYMENT.md       # 배포 문서 (본 문서)
```

---

## 🔄 배포 방법 (Vercel)

1. GitHub 저장소에 코드 push
2. Vercel에서 저장소 연결
3. 자동 배포 완료

변경 사항이 있을 때마다 `git push`하면 자동으로 배포됩니다.
